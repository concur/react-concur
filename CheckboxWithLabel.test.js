import React from 'react';
import CheckboxWithLabel from './CheckboxWithLabel';

test('CheckboxWithLabel changes the text after click', () => {
  // use enzyme to simulate the change event
  expect(false).toBe(true);
});
