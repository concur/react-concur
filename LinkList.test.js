import React from 'react';
import LinkList from './LinkList';

test('renders Links when passed an array of hrefs', () => {
  // use enzyme and enzyme-to-json to test with a shallow snapshot
  expect(false).toBe(true);
});